<Window x:Class="HotelUI.RegisterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hotel Management" Height="680" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" Background="Transparent" AllowsTransparency="True"
        Icon="icon.png" ShowInTaskbar="True">

    <Window.Resources>
        <Style x:Key="ModernInputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#F8F9FA"/><Setter Property="VerticalContentAlignment" Value="Center"/><Setter Property="Height" Value="45"/><Setter Property="Template">
                <Setter.Value><ControlTemplate TargetType="TextBox"><Grid><Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12"/><TextBlock x:Name="placeholder" Text="{TemplateBinding Tag}" Margin="15,0" Foreground="#A0AEC0" VerticalAlignment="Center" IsHitTestVisible="False"/><ScrollViewer x:Name="PART_ContentHost" Margin="5,0"/></Grid><ControlTemplate.Triggers><Trigger Property="IsFocused" Value="True"><Setter TargetName="placeholder" Property="Visibility" Value="Collapsed"/><Setter TargetName="border" Property="BorderBrush" Value="#10B981"/></Trigger><DataTrigger Binding="{Binding Text.IsEmpty, RelativeSource={RelativeSource Self}}" Value="False"><Setter TargetName="placeholder" Property="Visibility" Value="Collapsed"/></DataTrigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernPasswordStyle" TargetType="PasswordBox">
            <Setter Property="Background" Value="#F8F9FA"/><Setter Property="VerticalContentAlignment" Value="Center"/><Setter Property="Height" Value="45"/><Setter Property="Template">
                <Setter.Value><ControlTemplate TargetType="PasswordBox"><Grid><Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12"/><TextBlock x:Name="placeholder" Text="Password" Margin="15,0" Foreground="#A0AEC0" VerticalAlignment="Center" IsHitTestVisible="False"/><ScrollViewer x:Name="PART_ContentHost" Margin="5,0"/></Grid><ControlTemplate.Triggers><Trigger Property="IsFocused" Value="True"><Setter TargetName="placeholder" Property="Visibility" Value="Collapsed"/><Setter TargetName="border" Property="BorderBrush" Value="#10B981"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Border CornerRadius="20" ClipToBounds="True">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#10B981" Offset="0.0"/><GradientStop Color="#FFFFFF" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect><DropShadowEffect Color="#A0AEC0" BlurRadius="25" ShadowDepth="0" Opacity="0.25"/></Border.Effect>
            <StackPanel Margin="40" VerticalAlignment="Center">
                <Image Source="icon.png" Width="100" Height="100" HorizontalAlignment="Center" Margin="0,0,0,20" RenderOptions.BitmapScalingMode="HighQuality"/>
                <TextBlock Text="REGISTER" FontSize="26" FontWeight="ExtraBold" HorizontalAlignment="Center" Margin="0,0,0,30" Foreground="#047857"/>
                <TextBox x:Name="RegUser" Style="{StaticResource ModernInputStyle}" Tag="Username" Margin="0,0,0,15"/>
                <PasswordBox x:Name="RegPass" Style="{StaticResource ModernPasswordStyle}" Margin="0,0,0,30" PasswordChanged="RegPass_PasswordChanged"/>
                <Button Content="REGISTER" Height="50" Click="Register_Click" Background="#10B981" Foreground="White" FontWeight="Bold" Cursor="Hand">
                    <Button.Template><ControlTemplate TargetType="Button"><Border Background="{TemplateBinding Background}" CornerRadius="12"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border></ControlTemplate></Button.Template>
                </Button>
                <Button Content="Înapoi la Login" Click="Back_Click" Background="Transparent" BorderThickness="0" Foreground="#047857" FontWeight="SemiBold" Cursor="Hand" Margin="0,20,0,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <TextBlock x:Name="txt" Text="{TemplateBinding Content}" HorizontalAlignment="Center"/>
                                <Border x:Name="underline" Height="1" Background="#047857" VerticalAlignment="Bottom" Width="0" Margin="0,0,0,-2"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="txt" Property="Foreground" Value="#059669"/><Trigger.EnterActions><BeginStoryboard><Storyboard><DoubleAnimation Storyboard.TargetName="underline" Storyboard.TargetProperty="Width" To="100" Duration="0:0:0.2"/></Storyboard></BeginStoryboard></Trigger.EnterActions><Trigger.ExitActions><BeginStoryboard><Storyboard><DoubleAnimation Storyboard.TargetName="underline" Storyboard.TargetProperty="Width" To="0" Duration="0:0:0.2"/></Storyboard></BeginStoryboard></Trigger.ExitActions></Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>