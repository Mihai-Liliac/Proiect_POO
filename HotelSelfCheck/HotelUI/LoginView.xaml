<Window x:Class="HotelUI.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hotel Management" Height="680" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" Background="Transparent" AllowsTransparency="True"
        Icon="icon.png" ShowInTaskbar="True">

    <Window.Resources>
        <Style x:Key="ModernInputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="10,0,0,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12"/>
                            <TextBlock x:Name="placeholder" Text="{TemplateBinding Tag}" Margin="15,0" Foreground="#A0AEC0" VerticalAlignment="Center" IsHitTestVisible="False"/>
                            <ScrollViewer x:Name="PART_ContentHost" Margin="5,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="placeholder" Property="Visibility" Value="Collapsed"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#10B981"/>
                                <Setter TargetName="border" Property="Background" Value="White"/>
                            </Trigger>
                            <DataTrigger Binding="{Binding Text.IsEmpty, RelativeSource={RelativeSource Self}}" Value="False">
                                <Setter TargetName="placeholder" Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernPasswordStyle" TargetType="PasswordBox">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="10,0,0,0"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Grid>
                            <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12"/>
                            <TextBlock x:Name="placeholder" Text="Password" Margin="15,0" Foreground="#A0AEC0" VerticalAlignment="Center" IsHitTestVisible="False"/>
                            <ScrollViewer x:Name="PART_ContentHost" Margin="5,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="placeholder" Property="Visibility" Value="Collapsed"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#10B981"/>
                                <Setter TargetName="border" Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Background="Transparent">
                            <Border x:Name="HoverCircle" Background="White" CornerRadius="50" Opacity="0" Width="40" Height="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <ContentPresenter x:Name="btnContent" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="1,0,0,1" RenderTransformOrigin="0.5,0.5">
                                <ContentPresenter.Effect><DropShadowEffect Color="Black" BlurRadius="5" ShadowDepth="1" Opacity="0.3"/></ContentPresenter.Effect>
                                <ContentPresenter.RenderTransform>
                                    <TransformGroup>
                                        <ScaleTransform x:Name="scale" ScaleX="1" ScaleY="1"/>
                                        <RotateTransform x:Name="rotate" Angle="0"/>
                                    </TransformGroup>
                                </ContentPresenter.RenderTransform>
                            </ContentPresenter>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverCircle" Storyboard.TargetProperty="Opacity" To="0.2" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="scale" Storyboard.TargetProperty="ScaleX" To="1.2" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="scale" Storyboard.TargetProperty="ScaleY" To="1.2" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="rotate" Storyboard.TargetProperty="Angle" To="90" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverCircle" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="scale" Storyboard.TargetProperty="ScaleX" To="1" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="scale" Storyboard.TargetProperty="ScaleY" To="1" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="rotate" Storyboard.TargetProperty="Angle" To="0" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Storyboard x:Key="ShakeAnimation">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)">
                <EasingDoubleKeyFrame KeyTime="0:0:0.00" Value="0"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.10" Value="-12"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.20" Value="12"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.30" Value="-12"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.40" Value="12"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.50" Value="-6"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.60" Value="0"><EasingDoubleKeyFrame.EasingFunction><ElasticEase Oscillations="1" Springiness="5"/></EasingDoubleKeyFrame.EasingFunction></EasingDoubleKeyFrame>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
    </Window.Resources>

    <Grid Margin="20">
        <Border x:Name="MainCard" CornerRadius="20" ClipToBounds="True">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#10B981" Offset="0.0"/>
                    <GradientStop Color="#D1FAE5" Offset="0.6"/>
                    <GradientStop Color="#FFFFFF" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.RenderTransform><TranslateTransform X="0"/></Border.RenderTransform>
            <Border.Effect><DropShadowEffect Color="#A0AEC0" BlurRadius="25" ShadowDepth="0" Opacity="0.25"/></Border.Effect>
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="50"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <Button Content="✕" HorizontalAlignment="Right" Width="50" Height="50" Style="{StaticResource CloseButtonStyle}" Click="CloseButton_Click" Grid.Row="0" Margin="0,0,5,0"/>
                <StackPanel Grid.Row="1" Margin="40,0,40,40" VerticalAlignment="Center">
                    <Image Source="icon.png" Width="140" Height="140" HorizontalAlignment="Center" Margin="0,0,0,20" RenderOptions.BitmapScalingMode="HighQuality"/>
                    <TextBlock Text="HOTEL LOGIN" FontSize="26" FontWeight="ExtraBold" HorizontalAlignment="Center" Margin="0,0,0,8" Foreground="#047857"/>
                    <TextBlock Text="Introduceți datele pentru acces" Foreground="#718096" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,35"/>
                    <TextBox x:Name="UsernameBox" Style="{StaticResource ModernInputStyle}" Tag="Username" Margin="0,0,0,15"/>
                    <PasswordBox x:Name="PasswordBox" Style="{StaticResource ModernPasswordStyle}" PasswordChanged="PasswordBox_PasswordChanged" Margin="0,0,0,25"/>
                    <Border x:Name="ErrorBorder" Background="#FFF5F5" CornerRadius="8" Padding="10" Margin="0,0,0,20" Visibility="Collapsed" BorderBrush="#FEB2B2" BorderThickness="1">
                        <TextBlock x:Name="ErrorText" Foreground="#C53030" FontSize="13" TextWrapping="Wrap" HorizontalAlignment="Center"/>
                    </Border>
                    <Button Content="LOGIN" Height="50" Click="Login_Click" IsDefault="True" Background="#10B981" Foreground="White" FontWeight="Bold" FontSize="15" Cursor="Hand" Margin="0,0,0,15">
                        <Button.Template><ControlTemplate TargetType="Button"><Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="12"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="btnBorder" Property="Background" Value="#059669"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Button.Template>
                    </Button>
                    <Button Content="Nu ai cont? Înregistrează-te" Click="OpenRegister_Click" Background="Transparent" BorderThickness="0" Foreground="#047857" FontWeight="SemiBold" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Grid>
                                    <TextBlock x:Name="txt" Text="{TemplateBinding Content}" HorizontalAlignment="Center"/>
                                    <Border x:Name="underline" Height="1" Background="#047857" VerticalAlignment="Bottom" Width="0" Margin="0,0,0,-2"/>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="txt" Property="Foreground" Value="#059669"/><Trigger.EnterActions><BeginStoryboard><Storyboard><DoubleAnimation Storyboard.TargetName="underline" Storyboard.TargetProperty="Width" To="160" Duration="0:0:0.2"/></Storyboard></BeginStoryboard></Trigger.EnterActions><Trigger.ExitActions><BeginStoryboard><Storyboard><DoubleAnimation Storyboard.TargetName="underline" Storyboard.TargetProperty="Width" To="0" Duration="0:0:0.2"/></Storyboard></BeginStoryboard></Trigger.ExitActions></Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>